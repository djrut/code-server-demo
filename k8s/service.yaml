apiVersion: v1
kind: Service
metadata:
  name: code-server-djrut-sandbox
spec:
  type: NodePort
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: code-server-djrut-sandbox
